#include "main.h"
#include <string.h>

/**
 * _strlen -  a function to fint the length of a string
 * @s: param
 * Return: int
 */
int _strlen(char *s)
{
	int cnt;
	char str[] = s;

	cnt = strlen(str);
	return (cnt);
}
