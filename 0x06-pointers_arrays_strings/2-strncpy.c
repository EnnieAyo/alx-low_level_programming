#include "main.h"
#include <string.h>

/**
 * *_strncpy - a function to copy a string
 * @dest: param
 * @src: param
 * @n: param
 */
char *_strncpy(char *dest, char *src, int n)
{
char hold[4000];
int i;

        for (i = 0; i <= (int) strlen(s); i++)
        {
                hold[i] = s[i];
        }
	return (hold);
}
