#include "main.h"
#include <string.h>

/**
 * char *_strcat - a function to print the reverse of a string
 * @dest: param (destination)
 * @src: param (source)
 * Return: void
 */

char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
